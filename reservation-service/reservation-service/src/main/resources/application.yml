server:
  port: 8086

spring:
  application:
    name: reservation-service
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://localhost:3306/ticketflow_reservation?createDatabaseIfNotExist=true
    username: root
    password: root

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://ticketflow-mysql:3306/ticketflow_reservation?createDatabaseIfNotExist=true
    username: root
    password: root
